diff --git a/crates/metashrew-sync/src/sync.rs b/crates/metashrew-sync/src/sync.rs
index 914ddfc..c1454e1 100644
--- a/crates/metashrew-sync/src/sync.rs
+++ b/crates/metashrew-sync/src/sync.rs
@@ -503,7 +503,7 @@ where
                         Ok(tip) => tip,
                         Err(e) => {
                             error!("Failed to get tip height: {}", e);
-                            sleep(Duration::from_secs(5)).await;
+                            sleep(Duration::from_millis(800)).await;
                             continue;
                         }
                     };
@@ -528,7 +528,7 @@ where
                             }
                             Err(e) => {
                                 error!("Error handling reorg: {}", e);
-                                sleep(Duration::from_secs(5)).await;
+                                sleep(Duration::from_millis(800)).await;
                                 continue;
                             }
                         }
@@ -548,7 +548,7 @@ where
                             "Waiting for new blocks: current={}, tip={}",
                             current_height, remote_tip
                         );
-                        sleep(Duration::from_secs(3)).await;
+                        sleep(Duration::from_millis(500)).await;
                         continue;
                     }
 
@@ -556,7 +556,7 @@ where
                     {
                         let processing_heights = self_clone.processing_heights.lock().await;
                         if processing_heights.contains(&current_height) {
-                            sleep(Duration::from_millis(100)).await;
+                            sleep(Duration::from_millis(5)).await;
                             continue;
                         }
                     }
@@ -583,7 +583,7 @@ where
                         }
                         Err(e) => {
                             error!("Failed to fetch block {}: {}", current_height, e);
-                            sleep(Duration::from_secs(1)).await;
+                            sleep(Duration::from_millis(200)).await;
                         }
                     }
                 }
@@ -659,7 +659,7 @@ where
                             }
                             Err(e) => {
                                 error!("Failed to handle reorg: {}", e);
-                                sleep(Duration::from_secs(5)).await;
+                                sleep(Duration::from_millis(800)).await;
                                 failed_height
                             }
                         }
@@ -672,7 +672,7 @@ where
                         });
                     } else {
                         // Other errors: retry after delay
-                        sleep(Duration::from_secs(5)).await;
+                        sleep(Duration::from_millis(800)).await;
                         failed_height
                     }
                 }
diff --git a/crates/rockshrew-mono/src/adapters.rs b/crates/rockshrew-mono/src/adapters.rs
index e978c30..cb18336 100644
--- a/crates/rockshrew-mono/src/adapters.rs
+++ b/crates/rockshrew-mono/src/adapters.rs
@@ -68,8 +68,9 @@ impl BitcoinRpcAdapter {
 
     async fn request_with_retry<T: DeserializeOwned>(&self, method: &str, params: Vec<Value>) -> Result<T> {
         let max_retries = 5;
-        let mut retry_delay = Duration::from_millis(500);
-        let max_delay = Duration::from_secs(16);
+        // Cove local regtest: blocks arrive on demand, so retry almost at once.
+        let mut retry_delay = Duration::from_millis(10);
+        let max_delay = Duration::from_millis(100);
 
         let mut active_tunnel_guard = if self.tunnel_config.is_some() {
             Some(self.active_tunnel.lock().await)
@@ -140,7 +141,7 @@ impl BitcoinRpcAdapter {
                 method.hash(&mut hasher);
                 attempt.hash(&mut hasher);
                 let hash = hasher.finish();
-                (hash % 101) as u64 // 0-100 milliseconds, deterministic for same method+attempt
+                (hash % 11) as u64 // 0-10 milliseconds, deterministic for same method+attempt
             };
             retry_delay = std::cmp::min(max_delay, retry_delay * 2 + Duration::from_millis(jitter));
             tokio::time::sleep(retry_delay).await;
