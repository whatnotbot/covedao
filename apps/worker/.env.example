# Cove V3 worker (indexer + reconciliation) — deploy environment. Exactly
# these variables. The worker never asks the Guardian to sign; on mainnet it
# only compares its profile (fee address included) with the Guardian's at
# startup, and stops on a mismatch.

# regtest | signet | testnet | mainnet. Required, no default.
COVE_NETWORK=mainnet

# Postgres shared with the web app.
COVE_DATABASE_URL=

# Bitcoin Core JSON-RPC (API key in the URL for hosted providers).
# COVE_BITCOIN_RPC_USER / COVE_BITCOIN_RPC_PASSWORD are optional.
COVE_BITCOIN_RPC_URL=

# The Guardian, for the startup profile check (mainnet). Same values as the web's.
COVE_GUARDIAN_ENDPOINT=
COVE_GUARDIAN_AUTH_TOKEN=

# Where every protocol fee (mint, redeem, marketplace) is paid: a bc1… address
# you control. Set it ONCE as a shared variable: web, worker and Guardian must
# all have the same value. It is part of the profile hash, so a service with a
# different address refuses to run. Required on mainnet.
COVE_FEE_ADDRESS=

# Never set COVE_*_PRIVATE_KEY_HEX here: on mainnet the worker refuses them.
